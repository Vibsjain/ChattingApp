// import action types here
const { } = require('./actionTypes')

export const Action1 = (data) => {
    // return type and data to reducer
    return{
        type: '', data
    }
}
export const AsyncAction = () => dispatch => {
    dispatch({
        // action goes here
    })
}